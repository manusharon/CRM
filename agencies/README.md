# Agency generator

This script generates a CSV with synthetic agencies (Kerala & Tamil Nadu). Run locally and import into Postgres staging.

Requirements:
- pip install faker

Usage:
python generate_agencies.py

Outputs: agencies_kerala_tn.csv
